# UI-01R Independent Validation Report

- Validated commit: `4c440efbf5e5971e7a5a680d2dc8a8684ef4ea72`
- Branch: `ui/ui-01r-reference-reconciliation`
- Validator: independent Validation Codex
- Result: **FAIL — NOT ELIGIBLE FOR USER REVIEW**

## Checks

| Check | Result | Evidence |
|---|---|---|
| 1. 52-file manifest audit | PASS | `docs/14_SCREENSHOT_MANIFEST.md` lists 52 reference files; all 52 exist. |
| 2. 41 original screenshot mappings | PASS | `governance/ui_reference_mapping.json` has 52 entries and entries for every screenshot 1–41. |
| 3. Unmapped originals | PASS | Mapping metadata reports 0 unmapped originals. |
| 4. Screenshot-confirmed command coverage | **FAIL** | Ribbon inventory is substantially expanded, but the required quick-access/file command area is still absent and several required dialog fields are incomplete. |
| 5. Evidence set | PASS | 31 non-empty PNGs exist in `governance/ui_evidence/UI-01R/`; dimensions include 1440×900 and 1920×1080. |
| 6. Build | PASS | `npm run build` passes in `apps/desktop`. |
| 7. Runnable shell/evidence reproducibility | **FAIL** | `http://127.0.0.1:4175/` returns 200, but `apps/desktop/src/styles.css` is modified after the validated commit; the current live/evidence geometry cannot be reproduced from exact HEAD. |
| 8. P03+ engine exclusion | PASS | No scheduler/resource/tracking/persistence/import-export/report/consolidation engine detected in the desktop source. |
| 9. Unknown semantics | PASS | 26 questions remain `OPEN` in `docs/10_OPEN_QUESTIONS.md`. |
| 10. All Projects hierarchy | PASS | The fixture now contains 7 named projects plus the 3 program rows, with compact, selective and expanded outline modes in code. |
| 11. Structured views | PASS | Schedule grid/timeline, resource table and five-week calendar are represented as structured UI. |
| 12. Dependency pinning | PASS | Direct package versions are exact and match the lockfile. |
| 13. Windows/Tauri suitability | PASS | React/Vite shell remains acceptable for this review checkpoint and has no platform-specific blocker. |
| 14. S0/S1 defect check | **FAIL** | Missing quick-access architecture, incomplete required dialog structures and post-commit visual drift prevent eligibility. |

## Blocking findings

1. The title/quick-access area in `apps/desktop/src/main.jsx` contains only ProjectTrack branding, Help, Notifications and the avatar. It does not provide visible New, Open, Save, Save As, Undo, Redo, Print or Print Preview commands required by recovery prompt §6.1.
2. The Activity Information shell exposes only a short field list (`Row tab`, `Bars tab`, `Bar ID`, `Duration`, `Fixed Duration`, a few scheduled fields, `% Complete`, `Priority`, `Constraint Type`, `Calendar`). It does not visibly represent the required Revised and Actual tracking fields, constraint date/time, calendar New/Edit/Ignore Resource Calendars controls, or First/Previous/Next/Last navigation.
3. The Resource Information shell omits the required business/home addresses, phone/fax/mobile numbers, email addresses, URLs and IM addresses from its visible field model. Work Calendars also omits several required exception/shift/legend fields.
4. The checkout is dirty after commit `4c440ef`: `apps/desktop/src/styles.css` has an uncommitted row-geometry/QuickLook adjustment. The latest evidence captures therefore cannot be attributed reproducibly to the exact SHA under validation. Commit the final CSS, recapture affected evidence, and revalidate that new SHA.

## Verified interaction and artifact evidence

Earlier live checks verified ribbon switching, Resource and Calendar navigation, Summary Graph dialog opening/closing, compact schedule toggle and disabled future-command tooltips. Static checks verified corrected mapping paths (`bad_paths=0`), 52 source references, 31 evidence images, exact package pins, and a passing Vite build. These do not clear the blocking findings above.

## Scope

No P03 or later calculation engine was found. UI-01 remains reopened and P03+ remain blocked. Revalidation is required after the quick-access/dialog gaps and the post-commit CSS drift are resolved.
